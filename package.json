{
  "name": "channel-warder",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "deploy": "rm -rf .next out && next build && mkdir -p out && touch out/.nojekyll && echo 'warden-bot.headmade.pro' > out/CNAME && gh-pages -d out"
  },
  "dependencies": {
    "@ant-design/nextjs-registry": "^1.0.0",
    "@fontsource-variable/manrope": "^5.0.20",
    "antd": "^5.18.0",
    "gh-pages": "^6.1.1",
    "next": "14.2.3",
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "eslint": "^8",
    "eslint-config-next": "14.2.3",
    "typescript": "^5"
  }
}
